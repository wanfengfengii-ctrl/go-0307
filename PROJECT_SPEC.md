# 生物制药冻干机蒸汽灭菌验证与无菌放行

## 项目目标

建设一个带真实操作页面的 Go Web 服务，完成虚构生物制药冻干机从灭菌验证方案锁定、探头布置、蒸汽周期采集、致死率计算、偏差调查、复测到放行裁定的连续流程。系统以只追加事件保存每次验证代次、校准谱系、采样序列和裁定证据，使用嵌入式关系数据库、事务、唯一约束、快照与事件游标实现持久化和重启恢复。所有温度、压力、时间与致死率均采用整数定点算法和显式逻辑时钟。生产规模限定为20至30个Go文件、至少4个有实际职责的包、2000至3000行有效生产Go代码，目标约2500行；测试、前端、注释和生成填充不计入。前端采用固定依赖及锁文件构建并嵌入Go服务，可执行方案锁定、周期监控、偏差处理、双人复核和最终裁定。

## 端到端业务流程

1. 方案锁定：定义设备腔体、搁板、冷凝器、排水口、装载构型、探头位置、校准证书摘要、采样频率、暴露阶段、温压边界和致死率阈值；完成几何、版本和覆盖性校验后生成不可变验证代次。
2. 探头准备：为温度、压力和生物指示物建立逐件身份，校验校准有效期与量程；获取校准槽和数据采集通道的限时互斥租约，原子绑定探头、位置和当前代次。
3. 灭菌采集：按预热、置换、升温、暴露、排汽和冷却的严格阶段前缀接收带序号样本；检查租约、代次、采样间隔、设备回执及断点连续性，重复样本保持幂等。
4. 周期判定：使用整数定点运算计算每个位置的暴露累计值、基准温度等效致死率、最低温点、温度均匀性和压力对应偏差；只有时间轴完整、关键位置合格且生物指示物结果有效才可进入复核。
5. 偏差复测：断样、校准漂移、冷点不足、超压或生物指示物阳性触发确定性传播，按共享采集通道、相邻腔体区域、同校准批及同装载层生成排序去重的复测集合，并建立新代次。
6. 复核放行：两名不同且资格有效的人员独立复核；放行、隔离或作废请求争用唯一终局屏障，仅首个满足条件的请求形成可恢复裁定。

## 核心组件与职责

1. 验证方案与覆盖规则包：管理设备结构、装载构型、探头位置、阶段参数、校准摘要和阈值，执行版本、位置覆盖和整数边界校验。
2. 周期聚合与采集包：以事件维护验证代次、阶段前缀、探头绑定、样本序列和设备调用，拒绝断序、跳级及旧代次回执。
3. 探头谱系与租约包：维护探头、证书、生物指示物及位置关系，为校准槽和采集通道提供逻辑时钟驱动的互斥租约。
4. 致死率与偏差包：完成定点积分、均匀性判定、异常规范化、传播闭包、复测代次和双人复核。
5. 持久化与恢复包：用关系数据库事务保存事件、快照、幂等记录、租约、重试任务和唯一终局，启动时校验并重建状态。
6. HTTP与操作页面：提供JSON接口、稳定错误结构和嵌入式前端，页面通过接口展示并操作方案、实时样本、复测和裁定。

## 领域规则与不变量

1. 方案锁定后不得原位修改；结构、装载、探头或阈值变化必须创建新代次，陈旧摘要返回VALIDATION_STALE。
2. 探头编号全局唯一，同一探头在重叠逻辑时间内只能绑定一个位置；证书必须覆盖测量时刻和所需量程，过期或越界探头不得产生有效样本。
3. 周期阶段严格遵循预热、置换、升温、暴露、排汽、冷却前缀；暴露开始必须满足连续温度和压力条件，任何阶段不可跳过或覆盖。
4. 样本键由周期、代次、探头、序号组成且唯一；相同操作号与规范化内容重试返回原结果，内容不同返回IDEMPOTENCY_CONFLICT。各探头序号和逻辑时间必须严格递增。
5. 温度以千分之一摄氏度、压力以帕、时间以毫秒、致死率以百万分之一分钟表示；积分采用梯形法和半离零舍入，所有中间运算检查除零、负间隔和64位溢出。
6. 资源租约由资源、持有操作、代次、令牌及逻辑起止时刻定义；区间互斥，过期令牌不得写入有效证据。设备失败按固定策略追加有限次数重试。
7. 复测集合由异常源沿共享通道、相邻区域、同校准批和同装载层传播，成员按设备、区域、位置、探头、代次排序并计算摘要；相同摘要只能对应一个复测代次。
8. 旧代次迟到样本只进入审计事件，不得改变当前结果。放行要求时间轴完整、关键位置合格、生物指示物有效、复测闭合及两名不同合格人员独立复核；所有终局类型共享单写屏障。

## 数据模型与持久化

1. ValidationPlan：设备、代次、结构摘要、装载摘要、阶段参数、采样频率、阈值、锁定逻辑时刻和状态；关联ChamberRegion、ProbePosition与ExposureRule。
2. ProbeLineage：探头编号、类型、量程、证书摘要、校准批、有效区间和状态；ProbeBinding记录位置、代次与绑定区间。
3. CycleEvent：周期、代次、事件序号、阶段、逻辑时间、操作号和输入摘要；CycleSnapshot保存游标、状态及校验和。
4. Sample：探头、序号、逻辑时间、整数读数、设备回执摘要和有效性；BiologicalIndicator记录逐件身份、位置、培养结果与判读证据。
5. ResourceLease与DeviceCall：保存资源令牌、逻辑有效期、调用摘要、故障类别、重试次数、下一重试时刻和最终回执。
6. CalculationResult：位置累计致死率、最低温度、均匀性、压力偏差、输入区间及算法版本；结果只追加。
7. DeviationCase与RetestMember：异常来源、传播摘要、复测代次、规范化成员和完成状态。
8. Review、FinalDecision与IdempotencyRecord：保存复核资格、独立结论、唯一终局、凭据编号，以及操作号对应的请求与响应摘要。

## 公开接口

1. POST /api/v1/validations/lock锁定方案；GET /api/v1/validations/{id}返回按规范键排序的方案、代次和状态。
2. POST /api/v1/validations/{id}/probes/bind及POST /api/v1/leases/acquire、/renew、/release管理探头绑定和资源租约。
3. POST /api/v1/cycles/{id}/stages与POST /api/v1/cycles/{id}/samples提交阶段和样本；写请求携带operation_id、expected_generation和logical_time。
4. POST /api/v1/cycles/{id}/calculate执行确定性判定；GET /api/v1/cycles/{id}/results返回输入区间、计算值和规范化原因。
5. POST /api/v1/cycles/{id}/deviations、GET /api/v1/cycles/{id}/retests及POST /api/v1/retests/{id}/evidence管理异常闭包和复测证据。
6. POST /api/v1/cycles/{id}/reviews与POST /api/v1/cycles/{id}/final-decisions提交复核及终局竞争；GET /api/v1/cycles/{id}/audit返回只追加时间线。
7. 错误响应包含code、operation_id、retryable和已排序reasons；公共测试精确断言业务错误码。前端包含方案编辑、阶段时间线、探头矩阵、样本流、复测集合、复核及裁定区域，使用固定版本依赖、package.json和锁文件确定性构建。

## 失败边界

1. 方案锁定事务同时校验版本、位置覆盖、探头唯一性和阈值；失败不留下部分方案、位置或事件。
2. 绑定事务原子检查代次、证书、位置占用和租约，并写入绑定与事件；并发竞争由唯一约束和条件更新裁决。
3. 样本事务先验证阶段、序号、租约、代次和设备回执，再写样本与游标；断序、过期或旧代次不得推进聚合状态。
4. 计算只读取一个确定事件游标内的快照；发现缺样、越界或溢出时不写部分位置结果。
5. 复测创建锁定当前聚合版本，一次提交传播成员、摘要和新代次；冲突整体回滚，相同摘要返回既有结果。
6. 终局由唯一约束实现单写；崩溃后根据事务日志、事件游标和快照校验和恢复，未提交数据不可见，租约及重试次数不依赖墙钟。

## 验收标准

1. 方案锁定完整冻结设备结构、装载、位置、探头摘要、阶段和阈值；陈旧版本、位置遗漏、重复探头或整数越界稳定拒绝且不留部分数据。
2. 探头证书、量程、绑定区间和互斥租约均被验证；同步竞争同一位置或通道仅一方成功，失败事务整体回滚。
3. 六个周期阶段严格按前缀推进；跳步、断序、重复冲突、过期租约和旧代次回执均不会形成有效状态。
4. 致死率、均匀性和压力偏差使用规定定点算法，对舍入、阈值边界、负时间、除零及中间值溢出给出可精确断言的结果。
5. 设备断连、超时、格式错误和校准漂移形成固定次数的重试任务；进程重启后事件游标、租约、重试次数及下一逻辑时刻一致。
6. 异常传播生成排序去重且摘要唯一的复测集合；新代次生效后，旧代次样本不能改变当前结论。
7. 仅在时间轴、计算、生物指示物、复测和双人复核全部满足时形成唯一终局；同步并发请求只允许一个提交并可在重启后恢复。
8. 生产实现不少于20个Go文件和2000行有效生产Go代码，目标约2500行且不超过3000行；至少4个业务包。至少4个公共测试文件、12个公共测试，前端可确定性构建并由Go服务实际提供。

## 确定性测试场景

1. tests/plan_lock_test.go：成功锁定固定方案；陈旧摘要、位置遗漏、重复探头及尺寸溢出分别返回稳定错误，并断言数据库无部分记录。
2. tests/probe_lease_test.go：证书过期和量程不足拒绝；同步竞争同一通道仅一个租约成功；绑定失败回滚；相同操作幂等而变更内容冲突。
3. tests/cycle_sequence_test.go：六阶段正常闭合；跳过置换、样本断序、重复序号异值、过期令牌及旧代次回执均按预期处理。
4. tests/lethality_test.go：以固定温度序列验证梯形积分和半离零舍入；覆盖阈值等值、负间隔、除零和乘法溢出。
5. tests/recovery_test.go：脚本设备产生断连、超时、格式错误及漂移；关闭并重启服务后，快照游标、租约和重试队列保持一致。
6. tests/retest_test.go：异常沿四类关系传播，多路径重叠仅生成一个排序集合；并发触发只创建一个新代次，旧样本仅进入审计。
7. tests/final_decision_test.go：缺样、致死率不足、生物指示物无效、复测未闭合或复核人相同均拒绝；同步竞争三个终局请求仅一个成功。
8. 公共测试至少采用上述7个文件和不少于20个独立用例，统一使用固定探头序列、显式逻辑时钟、脚本设备和同步屏障，不使用睡眠、随机数或真实仪器。

## 组件追踪关系

1. 验证方案与覆盖规则包对应方案冻结、位置覆盖和整数边界，由plan_lock_test.go验证。
2. 周期聚合与采集包对应阶段前缀、样本序列和旧代次隔离，由cycle_sequence_test.go验证。
3. 探头谱系与租约包对应证书、绑定、资源竞争和事务原子性，由probe_lease_test.go验证。
4. 致死率与偏差包对应定点计算、异常传播和复测代次，由lethality_test.go与retest_test.go验证。
5. 持久化与恢复包对应事件游标、快照、租约及重试恢复，由recovery_test.go与final_decision_test.go验证。
6. HTTP与操作页面贯穿全部流程；接口集成测试断言稳定错误和事务结果，前端构建测试验证锁文件安装、资源生成、Go嵌入及页面对真实接口的读写。

## 独特性

项目围绕冻干机蒸汽周期的探头校准谱系、阶段化采样、整数致死率积分、冷点传播复测及无菌放行展开；共享通道与腔体区域共同驱动的复测闭包，以及旧代次样本隔离和终局竞争，形成专属且连续的验证工作流。
